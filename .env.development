# .env.development — 개발 환경 설정
# npm run dev (vite) 실행 시 자동으로 로드됩니다.
# Vite에서 브라우저 코드에 노출할 변수는 반드시 VITE_ 접두사를 붙여야 합니다.

# Spring Boot 로컬 서버 주소
VITE_API_BASE_URL=http://localhost:8080

# 현재 실행 환경 — 화면에 배지로 표시됩니다.
VITE_APP_ENV=development
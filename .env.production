# .env.production — 운영 환경 설정
# npm run build (vite build) 실행 시 자동으로 로드됩니다.
# 실제 배포 전 서버 주소로 변경하세요.

# Spring Boot 운영 서버 주소 (배포 전 실제 URL로 교체)
VITE_API_BASE_URL=https://your-api-server.com

# 현재 실행 환경 — 화면에 배지로 표시됩니다.
VITE_APP_ENV=production